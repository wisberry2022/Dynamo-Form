import { PageLayout, PageWithLayout, Radio, RadioGroup } from "@/5_shared";
import { useState } from "react";

const Page: PageWithLayout = () => {
  return <></>;
};

Page.getLayout = (page) => <PageLayout>{page}</PageLayout>;

export default Page;
